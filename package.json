{
  "name": "link-monitor-monorepo",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "postinstall": "npm --prefix apps/web install",
    "prisma:generate": "npm --prefix apps/web run prisma:generate",
    "build": "npm --prefix apps/web run build",
    "start": "npm --prefix apps/web run start"
  }
}
